services:
  dashboard:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ${DASHBOARD_DATA_DIR:-./data}:/app/data
      - ./static:/app/static
    restart: unless-stopped
    environment:
      - ORIGIN=${DASHBOARD_ORIGIN:-http://localhost:3000}
