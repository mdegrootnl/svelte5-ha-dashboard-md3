import type Home from '~icons/material-symbols/home';

/**
 * Type representing a Material Symbols icon component.
 * Use this instead of `any` for icon props.
 */
export type IconComponent = typeof Home;
